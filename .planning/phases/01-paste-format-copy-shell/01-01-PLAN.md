---
phase: 01-paste-format-copy-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/App.css
  - src/vite-env.d.ts
  - tsconfig.json
  - tsconfig.node.json
  - vite.config.ts
autonomous: true
requirements:
  - INPT-01
  - INPT-02
  - INPT-03
  - FORM-01
  - OUTP-01
must_haves:
  truths:
    - "User sees a single-page stacked layout with input above output, covering D-01 and D-03."
    - "User sees one top action bar containing Format, Copy, Download, Clear, and Sample controls, covering D-02."
    - "The UI uses a minimal light-neutral utility tone, covering D-04, D-05, and D-06."
  artifacts:
    - path: "src/App.tsx"
      provides: "Single-page formatter shell"
      min_lines: 80
    - path: "src/App.css"
      provides: "Stacked responsive utility layout"
      min_lines: 80
    - path: "package.json"
      provides: "Vite React TypeScript project scripts"
      contains: "\"dev\""
  key_links:
    - from: "src/main.tsx"
      to: "src/App.tsx"
      via: "React root render"
      pattern: "createRoot.*render"
---

<objective>
Scaffold the Vite React TypeScript app and build the initial static shell.

Purpose: Establish the first usable screen and project structure for the paste-format-copy formatter.
Output: Working app scaffold with stacked input/output panels, top action bar, sample and clear controls, and minimal light-neutral styling.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-paste-format-copy-shell/01-CONTEXT.md
@.planning/phases/01-paste-format-copy-shell/01-RESEARCH.md
@.planning/phases/01-paste-format-copy-shell/SKELETON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Vite React TypeScript scaffold</name>
  <files>package.json, package-lock.json, index.html, src/main.tsx, src/vite-env.d.ts, tsconfig.json, tsconfig.node.json, vite.config.ts</files>
  <read_first>.planning/phases/01-paste-format-copy-shell/01-RESEARCH.md, .planning/phases/01-paste-format-copy-shell/SKELETON.md</read_first>
  <action>Create a Vite React TypeScript project at the repo root. Use scripts: "dev": "vite", "build": "tsc -b && vite build", "preview": "vite preview", "test": "vitest run". Do not add routing, server code, auth, database, or storage. Keep the root route as the app.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - package.json contains "vite", "react", "typescript", and "vitest".
    - index.html mounts to a root element.
    - src/main.tsx renders App.
  </acceptance_criteria>
  <done>Project scaffold builds successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Build stacked formatter shell</name>
  <files>src/App.tsx, src/App.css</files>
  <read_first>.planning/phases/01-paste-format-copy-shell/01-CONTEXT.md</read_first>
  <action>Implement the initial App component with a top action bar and two stacked panels. The first panel is a large LaTeX source textarea. The second panel is an output area that preserves whitespace and line breaks. Add Format, Copy, Download, Clear, and Sample controls to the single top action bar. Copy and Download can be disabled until Plan 03 wires behavior, but the controls must be visible. Use labels rather than icon-only controls in Phase 1.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - src/App.tsx contains visible controls named Format, Copy, Download, Clear, and Sample.
    - src/App.tsx renders input before output in DOM order.
    - src/App.css defines stacked panels and responsive behavior without side-by-side desktop layout.
  </acceptance_criteria>
  <done>Static shell matches D-01, D-02, D-03, D-04, D-05, and D-06.</done>
</task>

<task type="auto">
  <name>Task 3: Wire sample and clear state</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx</read_first>
  <action>Add local React state for input and output. Implement Sample to load a LaTeX snippet with a document environment, item list, and display math. Implement Clear to empty input, output, status, and diagnostics. Do not persist pasted input to localStorage.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - Sample button loads non-empty LaTeX containing "\\begin{document}" and "\\item".
    - Clear button clears both input and output state.
    - No localStorage or sessionStorage usage is introduced.
  </acceptance_criteria>
  <done>User can load a sample and clear the page without refresh.</done>
</task>

</tasks>

<verification>
- [ ] npm run build succeeds
- [ ] App shows input above output
- [ ] Top action bar includes Format, Copy, Download, Clear, and Sample
- [ ] Sample and Clear work from local state
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- INPT-01, INPT-02, INPT-03, FORM-01, and OUTP-01 have visible shell support
- D-01 through D-06 are reflected in the UI
</success_criteria>

<output>
After completion, create `.planning/phases/01-paste-format-copy-shell/01-01-SUMMARY.md`
</output>
