---
phase: 01-paste-format-copy-shell
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/App.tsx
  - src/App.css
  - src/__tests__/App.test.tsx
  - src/test/setup.ts
  - package.json
  - vite.config.ts
  - README.md
autonomous: true
requirements:
  - OUTP-02
  - OUTP-03
must_haves:
  truths:
    - "Format is visually and behaviorally primary, covering D-10."
    - "Copy is a secondary action enabled only when output exists, covering D-11."
    - "Download, Clear, and Sample are quieter secondary actions in the top action bar, covering D-12."
    - "Toolbar status text reports formatting feedback without toast or output banner UI, covering D-13."
  artifacts:
    - path: "src/__tests__/App.test.tsx"
      provides: "UI flow coverage for copy/download/status behavior"
      min_lines: 50
    - path: "README.md"
      provides: "Local run and verification instructions"
      min_lines: 20
  key_links:
    - from: "src/App.tsx"
      to: "navigator.clipboard.writeText"
      via: "Copy action"
      pattern: "clipboard\\.writeText"
    - from: "src/App.tsx"
      to: "Blob"
      via: "Download action"
      pattern: "new Blob"
---

<objective>
Finish output actions, status feedback, responsive polish, and README instructions for the Phase 1 shell.

Purpose: Complete the user-visible paste-format-copy loop and make it verifiable.
Output: Working Copy and Download actions, toolbar status text, responsive polish, UI tests, and README.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-paste-format-copy-shell/01-CONTEXT.md
@.planning/phases/01-paste-format-copy-shell/01-RESEARCH.md
@.planning/phases/01-paste-format-copy-shell/01-01-SUMMARY.md
@.planning/phases/01-paste-format-copy-shell/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement copy and download actions</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx</read_first>
  <action>Implement Copy using `navigator.clipboard.writeText(output)` and guard it so the button is disabled when output is empty. Implement Download by creating a `Blob` with MIME type `text/x-tex;charset=utf-8`, creating an object URL, clicking a temporary anchor with filename `formatted.tex`, then revoking the URL. Keep Copy secondary and Format primary through class names or button variants.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - Copy button is disabled when output is empty.
    - Copy handler calls navigator.clipboard.writeText with output.
    - Download handler creates a Blob and filename "formatted.tex".
  </acceptance_criteria>
  <done>Formatted output can be copied and downloaded.</done>
</task>

<task type="auto">
  <name>Task 2: Add status and responsive polish</name>
  <files>src/App.tsx, src/App.css</files>
  <read_first>src/App.tsx, src/App.css, .planning/phases/01-paste-format-copy-shell/01-CONTEXT.md</read_first>
  <action>Render short toolbar status text for Format, Copy, Download, Clear, Sample, empty input, no changes, and diagnostics. Do not use toasts or output banners. Update CSS so the top action bar wraps cleanly on mobile, buttons do not overflow, panels keep stable min-heights, and input remains above output at all viewport widths.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - App.tsx contains status strings "Formatted", "No changes", and "Check diagnostics".
    - App.css uses flex-wrap or equivalent responsive wrapping for the toolbar.
    - No toast component or output banner is introduced.
  </acceptance_criteria>
  <done>Toolbar feedback and responsive behavior match context decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Add UI flow tests and README</name>
  <files>src/__tests__/App.test.tsx, src/test/setup.ts, package.json, vite.config.ts, README.md</files>
  <read_first>src/App.tsx, package.json, vite.config.ts</read_first>
  <action>Add React Testing Library tests for loading sample input, formatting, enabling Copy/Download after output exists, clearing state, and showing toolbar status. Mock `navigator.clipboard.writeText` and URL object methods where needed. Add README instructions for `npm install`, `npm run dev`, `npm run build`, and `npm test`, plus a short note that Phase 1 formatting is best-effort.</action>
  <verify>npm run build && npm test</verify>
  <acceptance_criteria>
    - App.test.tsx exercises sample -> format -> copy/download enabled -> clear.
    - README.md documents local run, build, and test commands.
    - README.md states formatting is best-effort in the first version.
  </acceptance_criteria>
  <done>Output actions and Phase 1 verification are covered.</done>
</task>

</tasks>

<verification>
- [ ] npm run build succeeds
- [ ] npm test succeeds
- [ ] Copy and Download are disabled until output exists
- [ ] Status text appears in the toolbar
- [ ] README explains local run/test/build
</verification>

<success_criteria>
- All tasks completed
- OUTP-02 and OUTP-03 are functional
- D-10, D-11, D-12, and D-13 are implemented and tested
- Phase 1 paste-format-copy shell is ready for execution verification
</success_criteria>

<output>
After completion, create `.planning/phases/01-paste-format-copy-shell/01-03-SUMMARY.md`
</output>
