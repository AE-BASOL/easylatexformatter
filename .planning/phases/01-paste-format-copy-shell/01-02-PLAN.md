---
phase: 01-paste-format-copy-shell
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/lib/formatter.ts
  - src/App.tsx
  - src/__tests__/formatter.test.ts
  - src/test/setup.ts
  - vite.config.ts
autonomous: true
requirements:
  - FORM-01
  - OUTP-01
must_haves:
  truths:
    - "Clicking Format applies basic LaTeX indentation rather than normalize-only behavior, covering D-07 and D-08."
    - "Formatter returns diagnostics that state best-effort limitations, covering D-09."
    - "Output preserves whitespace and line breaks in the output panel."
  artifacts:
    - path: "src/lib/formatter.ts"
      provides: "Formatter adapter and best-effort LaTeX formatting"
      min_lines: 80
      exports:
        - "formatLatexSource"
    - path: "src/__tests__/formatter.test.ts"
      provides: "Formatter fixture coverage"
      min_lines: 40
  key_links:
    - from: "src/App.tsx"
      to: "src/lib/formatter.ts"
      via: "Format button calls formatLatexSource"
      pattern: "formatLatexSource"
---

<objective>
Implement the Phase 1 deterministic formatter adapter and wire it to the Format action.

Purpose: Make the shell provide real value by formatting common LaTeX structures while staying honest about limits.
Output: `src/lib/formatter.ts`, formatter tests, and App wiring from input to formatted output.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-paste-format-copy-shell/01-CONTEXT.md
@.planning/phases/01-paste-format-copy-shell/01-RESEARCH.md
@.planning/phases/01-paste-format-copy-shell/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create formatter adapter</name>
  <files>src/lib/formatter.ts</files>
  <read_first>.planning/phases/01-paste-format-copy-shell/01-CONTEXT.md, .planning/research/ARCHITECTURE.md, .planning/research/PITFALLS.md</read_first>
  <action>Create `FormatOptions` and `FormatResult` types plus `formatLatexSource(input: string, options?: Partial<FormatOptions>): FormatResult`. Implement best-effort formatting that normalizes CRLF to LF, removes trailing whitespace, preserves blank lines, indents after `\begin{...}`, de-indents before `\end{...}`, and indents `\item` under list environments. Add diagnostics including "Best-effort formatter: advanced LaTeX formatting comes later." Protect simple verbatim-like blocks named `verbatim`, `lstlisting`, and `minted` by passing their inner lines through unchanged.</action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - formatter.ts exports FormatOptions, FormatResult, and formatLatexSource.
    - formatLatexSource returns diagnostics with a best-effort limitation notice.
    - formatter.ts contains protected environment names "verbatim", "lstlisting", and "minted".
  </acceptance_criteria>
  <done>Formatter adapter returns output, diagnostics, and changed status.</done>
</task>

<task type="auto">
  <name>Task 2: Add formatter fixture tests</name>
  <files>src/__tests__/formatter.test.ts, src/test/setup.ts, vite.config.ts</files>
  <read_first>src/lib/formatter.ts, package.json, vite.config.ts</read_first>
  <action>Configure Vitest with jsdom only if needed by existing config. Add formatter tests for nested document/enumerate blocks, `\item` indentation, trailing space cleanup, unchanged empty input handling, and verbatim-like block preservation. Keep tests deterministic and independent of browser APIs.</action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - npm test runs formatter tests.
    - Tests assert indentation around "\\begin{document}" and "\\end{document}".
    - Tests assert verbatim-like content remains unchanged.
  </acceptance_criteria>
  <done>Formatter behavior is protected by unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Format action to UI</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx, src/lib/formatter.ts</read_first>
  <action>Update the Format button handler to validate non-empty input, call `formatLatexSource`, set output to the returned output, render diagnostics, and set toolbar status to "Formatted", "No changes", or "Check diagnostics" as appropriate. Do not mutate the input textarea during formatting.</action>
  <verify>npm run build && npm test</verify>
  <acceptance_criteria>
    - App.tsx imports formatLatexSource.
    - Format handler leaves input state unchanged.
    - Output panel displays formatter output after Format.
    - Empty input produces a diagnostic/status instead of crashing.
  </acceptance_criteria>
  <done>Format action drives output through the adapter.</done>
</task>

</tasks>

<verification>
- [ ] npm run build succeeds
- [ ] npm test succeeds
- [ ] Formatter tests cover nested environments, items, trailing whitespace, empty input, and protected blocks
- [ ] UI shows formatted output without mutating input
</verification>

<success_criteria>
- All tasks completed
- FORM-01 and OUTP-01 are functional, not just visible
- D-07, D-08, and D-09 are implemented and tested
</success_criteria>

<output>
After completion, create `.planning/phases/01-paste-format-copy-shell/01-02-SUMMARY.md`
</output>
